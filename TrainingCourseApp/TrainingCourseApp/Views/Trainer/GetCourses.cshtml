@model IEnumerable<TrainingCourseApp.Models.Course>
@{
    ViewBag.Title = "GetCourses";
}

<h2>My Courses</h2>

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Course</th>
            <th scope="col">Description</th>
            <th scope="col">Category</th>
            <th scope="col">Trainees</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in @Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Category.Name</td>
                <td>
                    @Html.ActionLink("Trainees", "TraineesInCourse", "Trainer", new { id = item.Id }, new { @class = "btn btn-success" })
                </td>
            </tr>
        }
    </tbody>
</table>